import React from 'react';
import Main from '../main/main.jsx';

// eslint-disable-next-line react/prop-types
const App = ({genre, releaseDate}) => <Main genre={genre} releaseDate={releaseDate}/>;

export default App;
